syntax = "proto3";

package tivet.backend.pkg.job_log.msg.export_complete;

import "resources/legacy/proto/common.proto";

/// name = "msg-job-log-export-complete"
/// parameters = [
/// 	{ name = "request_id" },
/// ]
message Message {
	tivet.common.Uuid request_id = 1;
	tivet.common.Uuid upload_id = 2;
}

