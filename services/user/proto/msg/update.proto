syntax = "proto3";

package tivet.backend.pkg.user.msg.update;

import "resources/legacy/proto/common.proto";

/// name = "msg-user-update"
/// tail-ttl = 300
/// parameters = [
/// 	{ name = "user_id" },
/// ]
message Message {
	tivet.common.Uuid user_id = 1;
}
