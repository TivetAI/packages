syntax = "proto3";

package tivet.backend.pkg.upload.msg.delete;

import "resources/legacy/proto/common.proto";

/// name = "msg-upload-delete"
/// parameters = [
/// 	{ name = "request_id" },
/// ]
message Message {
	tivet.common.Uuid request_id = 1;
	repeated tivet.common.Uuid upload_ids = 2;
}
