syntax = "proto3";

package tivet.backend.pkg.upload.msg.complete_complete;

import "resources/legacy/proto/common.proto";

/// name = "msg-upload-complete-complete"
/// parameters = [
/// 	{ name = "upload_id" },
/// ]
message Message {
	tivet.common.Uuid upload_id = 1;
}

