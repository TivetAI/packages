syntax = "proto3";

package tivet.backend.pkg.nomad.msg.monitor_node_registered;

import "resources/legacy/proto/common.proto";

/// name = "msg-nomad-monitor-node-registered"
/// parameters = [
/// 	{ name = "server_id" },
/// ]
message Message {
	tivet.common.Uuid server_id = 1;
	string node_id = 2;
}
