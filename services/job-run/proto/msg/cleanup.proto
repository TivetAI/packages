syntax = "proto3";

package tivet.backend.pkg.job_run.msg.cleanup;

import "resources/legacy/proto/common.proto";

/// name = "msg-job-run-cleanup"
/// deduplicate = true
/// parameters = [
/// 	{ name = "run_id" },
/// ]
message Message {
	reserved 2;

	tivet.common.Uuid run_id = 1;
}

