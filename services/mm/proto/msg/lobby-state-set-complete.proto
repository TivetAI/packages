syntax = "proto3";

package tivet.backend.pkg.mm.msg.lobby_state_set_complete;

import "resources/legacy/proto/common.proto";

/// name = "msg-mm-lobby-state-set-complete"
/// parameters = [
/// 	{ name = "lobby_id" },
/// ]
message Message {
	tivet.common.Uuid lobby_id = 1;
}
