syntax = "proto3";

package tivet.backend.pkg.mm.msg.lobby_closed_set_complete;

import "resources/legacy/proto/common.proto";

/// name = "msg-mm-lobby-closed-set-complete"
/// parameters = [
/// 	{ name = "lobby_id" },
/// ]
message Message {
	tivet.common.Uuid lobby_id = 1;
}

