syntax = "proto3";

package tivet.backend.pkg.mm.msg.lobby_ready_complete;

import "resources/legacy/proto/common.proto";

/// name = "msg-mm-lobby-ready-complete"
/// parameters = [
/// 	{ name = "lobby_id" },
/// ]
message Message {
	tivet.common.Uuid lobby_id = 1;
}

