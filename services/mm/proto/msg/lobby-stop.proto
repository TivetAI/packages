syntax = "proto3";

package tivet.backend.pkg.mm.msg.lobby_stop;

import "resources/legacy/proto/common.proto";

/// name = "msg-mm-lobby-stop"
/// deduplicate = true
/// parameters = [
/// 	{ name = "lobby_id" },
/// ]
message Message {
	tivet.common.Uuid lobby_id = 1;
}

