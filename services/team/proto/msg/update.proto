syntax = "proto3";

package tivet.backend.pkg.team.msg.update;

import "resources/legacy/proto/common.proto";

/// name = "msg-team-update"
/// tail-ttl = 300
/// parameters = [
/// 	{ name = "team_id" },
/// ]
message Message {
	tivet.common.Uuid team_id = 1;
}

