syntax = "proto3";

package tivet.backend.pkg.cf_custom_hostname.msg.create_complete;

import "resources/legacy/proto/common.proto";

/// name = "msg-cf-custom-hostname-create-complete"
/// parameters = [
/// 	{ name = "namespace_id" },
/// 	{ name = "hostname" },
/// ]
message Message {
    tivet.common.Uuid namespace_id = 1;
	string hostname = 2;
    tivet.common.Uuid identifier = 3;
}

