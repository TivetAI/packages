syntax = "proto3";

package tivet.backend.pkg.team_invite.msg.consume;

import "resources/legacy/proto/common.proto";

/// name = "msg-team-invite-consume"
/// parameters = [
/// 	{ name = "code" },
/// 	{ name = "user_id" },
/// ]
message Message {
	tivet.common.Uuid user_id = 1;
	string code = 2;
}

