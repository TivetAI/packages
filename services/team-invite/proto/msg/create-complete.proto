syntax = "proto3";

package tivet.backend.pkg.team_invite.msg.create_complete;

import "resources/legacy/proto/common.proto";

/// name = "msg-team-invite-create-complete"
/// parameters = [
/// 	{ name = "team_id" },
/// ]
message Message {
	string code = 1;
}

